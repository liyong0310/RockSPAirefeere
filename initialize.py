import cv2  # OpenCV，用于图像处理和显示
import numpy as np  # NumPy，用于数值计算
from ultralytics import YOLO  # Ultralytics YOLO，用于加载和运行 YOLO 模型
from PIL import Image  # Pillow，用于图像处理（可选）
import matplotlib.pyplot as plt  # Matplotlib，用于可视化（可选）
print('hello')